#pragma once

#include <stdint.h>

void xorEncrypt(char* input, uint8_t key);

// function signature 
// forward declaration
// declaration (function)
// Instruct the compiler that there exists a function with this signature (parameters + return)
//  but it does not define it!

// Look deeply into how pointers work
// look into the stack
// scope (function scope)
// pointers are set, referenced, and dereferenced